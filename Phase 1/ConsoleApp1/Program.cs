using System;
using System.Globalization;
using System.IO;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            char[] letters = { 'J', 'O', 'R', 'D', 'A', 'N' };
            for (int i = 0; i < 6; i++)
            {
                Console.WriteLine(letters[i]);
            }
        }
    }
}
